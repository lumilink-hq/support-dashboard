{
  "name": "seo-render-service",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone headless-browser rendering service (module 6's JS-rendered-site fallback). Deployed separately from the Next app, on Railway, from this directory's Dockerfile.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "tsx test/test-ssrf-guard.ts"
  },
  "dependencies": {
    "express": "5.2.1",
    "playwright": "1.63.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
